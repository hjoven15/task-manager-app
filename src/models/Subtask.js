export default class Subtask {

  constructor(id, name, detail, assignedUser, state) {
    this.IdSubtask = id;
    this.NameSubtask = name;
    this.DetailSubtask = detail;
    this.AssignedUser = assignedUser;
    this.StateSubtask = state;
  }
}
